[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['rm -rf node_modules package-lock.json', 'npm install --production']

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'npm start'